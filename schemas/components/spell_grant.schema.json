{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "spell_grant.schema.json",
    "title": "D&D 5e Spell Grant",
    "description": "Spells granted by a class, race, background, or feat, including guaranteed spells and choices of eligible spells.",
    "$comment": "version 0.3.0",
    "type": "object",
    "properties": {
        "spells": {
            "description": "Specific spells guaranteed by the grant.",
            "type": "array",
            "items": {
                "type": "string",
                "minLength": 1
            },
            "minItems": 1,
            "uniqueItems": true
        },
        "choices": {
            "description": "Choices of spells available through the grant.",
            "type": "array",
            "items": {
                "$ref": "../components/spell_choice.schema.json"
            },
            "minItems": 1
        },
        "casting_ability": {
            "description": "The ability used to cast spells granted by this grant.",
            "oneOf": [
                {
                    "$ref": "../values/ability_score.schema.json"
                },
                {
                    "type": "string",
                    "enum": [
                        "ability_score_choice"
                    ]
                }
            ]
        },
        "uses": {
            "description": "The number of times the granted spells can be cast without expending a spell slot.",
            "type": "integer",
            "minimum": 1
        },
        "recharge": {
            "description": "When the granted spells' uses are restored.",
            "$ref": "../values/duration.schema.json"
        }
    },
    "anyOf": [
        {
            "required": [
                "spells"
            ]
        },
        {
            "required": [
                "choices"
            ]
        }
    ],
    "additionalProperties": false
}