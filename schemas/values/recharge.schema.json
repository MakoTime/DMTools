{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "values/recharge.schema.json",
    "title": "D&D 5e Recharge",
    "description": "When uses of a feature, spell grant, or spell slot are restored.",
    "$comment": "version 0.4.0",
    "anyOf": [
        {
            "type": "string",
            "enum": [
                "short_rest",
                "long_rest",
                "dawn",
                "dusk",
                "round"
            ]
        },
        {
            "$ref": "custom_value.schema.json"
        }
    ]
}
